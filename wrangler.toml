name = "abramsimon-com"
compatibility_date = "2025-02-01"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "dist"

# Required by @astrojs/cloudflare for session support (optional; create with: wrangler kv:namespace create SESSION)
# [[kv_namespaces]]
# binding = "SESSION"
# id = "YOUR_KV_NAMESPACE_ID"

[vars]
# Add public env vars here if needed.

# Stripe secret key: set via `wrangler secret put STRIPE_SECRET_KEY`
# or in Cloudflare Dashboard → Workers & Pages → your project → Settings → Variables and Secrets
