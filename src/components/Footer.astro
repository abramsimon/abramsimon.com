---
const today = new Date();
---

<footer>
	<p class="tagline">
		Serving slow coffee & spinning warm records since {today.getFullYear()}.
	</p>
	<div class="social-links">
		<a href="#">
			<span class="sr-only">View Brew &amp; Groove playlist</span>
			<span>Night Spin Playlist</span>
		</a>
		<a href="#">
			<span class="sr-only">Contact Brew &amp; Groove</span>
			<span>Book a listening session</span>
		</a>
	</div>
</footer>
<style>
	footer {
		padding: 2.5em 1em 4em 1em;
		background: radial-gradient(circle at top, rgba(var(--mint), 0.15), transparent 55%),
			linear-gradient(180deg, rgba(var(--coffee), 0.98), rgba(var(--coffee-dark), 1));
		color: rgba(255, 249, 238, 0.9);
		text-align: center;
	}

	.tagline {
		margin: 0;
		font-size: 0.95rem;
	}

	.social-links {
		display: flex;
		justify-content: center;
		gap: 1em;
		margin-top: 1em;
	}
	.social-links a {
		text-decoration: none;
		color: rgba(255, 249, 238, 0.9);
		font-size: 0.85rem;
		text-transform: uppercase;
		letter-spacing: 0.16em;
		border-radius: 999px;
		padding: 0.4rem 0.9rem;
		border: 1px solid rgba(var(--mint), 0.6);
		background: rgba(0, 0, 0, 0.15);
	}
	.social-links a:hover {
		color: #f6fff8;
		background: rgba(0, 0, 0, 0.25);
	}
</style>
